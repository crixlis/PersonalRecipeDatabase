@model List<Recipe>;

@{
    ViewData["Title"] = "Recipes";
}
<h1>@ViewData["Title"]</h1>

<p>Take a look at your recipes</p>

@if(Model == null || !Model.Any()){
    <span>No recipes found!</span>
} else {
    <span>Number of recipes found: @Model.Count()</span>

    <table class="table">
    <thead>
        <tr>
        <th scope="col">Name</th>
        <th scope="col">Rating</th>
        <th scope="col">Creation time</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var recipe in Model)
        {
            <tr>
            <td>@recipe.Name</td>
            <td>@recipe.Rating</td>
            <td>@recipe.CreationTimeInMinutes</td>
            </tr>
        }

    </tbody>
    </table>
}